spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:password_storage
    driverClassName: org.h2.Driver
    username: admin
    password: nimda
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
